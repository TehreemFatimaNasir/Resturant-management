@{
    ViewData["Title"] = "Success";
}

<main>
    <h2>Booking Confirmed!</h2>
    <p>Your reservation has been successfully booked.</p>
    <a href="@Url.Action("Index", "Template")">Go back to Home</a>
</main>

<style>
    /* Reset margins and paddings */
    body, html {
        margin: 0;
        padding: 0;
        height: 100%;
    }

    /* Header Styling */
    header {
        position: relative;
        width: 100%;
        height: 80px; /* Adjust header height */
        background-color: #000; /* Example background */
        color: white;
        text-align: center;
        line-height: 80px;
    }

    /* Main Content */
    main {
        padding: 50px 20px; /* Adjust spacing */
        min-height: calc(100vh - 160px); /* Ensures it fits between header & footer */
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        text-align: center;
    }

    /* Footer Styling */
    footer {
        position: relative;
        width: 100%;
        height: 80px; /* Adjust footer height */
        background-color: #000; /* Example background */
        color: white;
        text-align: center;
        line-height: 80px;
    }
</style>
